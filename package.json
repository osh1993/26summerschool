{
  "name": "26summerschool-operations",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-public-data.mjs docs/data/sample.json",
    "build:wiki": "node scripts/build-wiki.mjs docs/data/sample.json wiki/Current-Status.md",
    "check": "npm run validate:data && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
